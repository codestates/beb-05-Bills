const tokenAbi = require("./tokenAbi.json");
const tokenAddress = require("./tokenAddress.json");
const couponAbi = require("./couponAbi.json");
const couponAddress = require("./couponAddress.json");
const nftAbi = require("./nftAbi.json");
const nftAddress = require("./nftAddress.json");

module.exports = {
  tokenAbi,
  tokenAddress,
  nftAbi,
  nftAddress,
  couponAbi,
  couponAddress,
};
